"""
Entry point for `python -m uiea_thirdhand_vla` / `thirdhand-vla` CLI.

Starts the web server and main control loop.
"""


def main():
    """Parse CLI args, load config, initialize subsystems, start server."""
    print("UIEA ThirdHand VLA v2.0.0 — starting...")
    print("(Framework skeleton — implementation to be pushed later)")


if __name__ == "__main__":
    main()
