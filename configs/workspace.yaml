# ============================================================
# Workspace Configuration — Safety Boundaries
# ============================================================
# All positions in meters, robot base frame.

bounds_m:
  x: [-0.3, 0.5]
  y: [-0.4, 0.4]
  z: [-0.05, 0.4]

desk_plane:
  normal: [0, 0, 1]
  offset_m: 0.0

keep_out_zones: []
  # - name: "camera_mount"
  #   type: "box"
  #   center_m: [0.3, -0.2, 0.25]
  #   half_extents_m: [0.05, 0.05, 0.1]

max_linear_speed_ms: 0.5
max_joint_speed_degs: 90.0
motion_timeout_s: 10.0
