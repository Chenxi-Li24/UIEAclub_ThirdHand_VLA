# Authorization

状态：**仅规划脚手架。**

本目录将实现物理任务的一次性授权验证。授权至少绑定计划 ID、计划修订、稳定目标身份、风险摘要、有效期和操作者确认，任何字段变化都必须使旧授权失效。

授权成功不代表动作安全，也不替代 Robot Service 的限位、速度、反馈新鲜度和运动互斥检查。授权必须一次性消费，并留下可审计结果。

迁移任务见 [`../../docs/MIGRATION_BACKLOG.md`](../../docs/MIGRATION_BACKLOG.md)。
