schema: thirdhand.skill-manifest.v1
id: vision.describe-scene
version: 0.1.0
summary: Generate a structured description of the current RGB-D scene.
risk: read-only
lifecycle: worker
runtime: node
operations: [invoke]
requires:
  services: [vision]
  devices: [xvisio]
  models: []
schemas:
  input: schemas/input.json
  result: schemas/result.json
entrypoint: src/worker.js
healthcheck: status
