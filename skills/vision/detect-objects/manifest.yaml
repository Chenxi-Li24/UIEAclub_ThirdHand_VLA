schema: thirdhand.skill-manifest.v1
id: vision.detect-objects
version: 0.1.0
summary: Detect and classify objects while returning stable target references.
risk: read-only
lifecycle: worker
runtime: node
operations: [invoke]
requires:
  services: [vision]
  devices: [xvisio]
  models: []
schemas:
  input: schemas/input.json
  result: schemas/result.json
entrypoint: src/worker.js
healthcheck: status
