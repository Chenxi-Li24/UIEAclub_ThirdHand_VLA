"""
Configuration management  YAML loading with Pydantic validation.

All configs live in configs/ as YAML files. This module validates them
at load time so errors are caught before any hardware is touched.
"""
